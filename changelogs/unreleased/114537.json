{
  "title": "Speed up portal-wide workspace lookups",
  "type": "change",
  "scope": ["core"],
  "description": "Cross-cutting performance work shared by every sub-app. The workspace lookup (findWorkspace), previously run several times per request (shared layout, page metadata and page render), is now request-memoized through getWorkspace so it hits the database once per request. Existing access and privacy rules are unchanged."
}
